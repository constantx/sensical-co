{{> header }}

  <header class='main-header'>
    <nav>
      <ul class="nav-items">
        {{#nav-main}}
        <li class="nav-item">
          <a href="{{href}}">{{label}}</a>
        </li>
        {{/nav-main}}
      </ul>
    </nav>
  </header>

  <div class="main-content">

    <section class="vcontainer section-welcome">
      <div class="container">
        {{#identity}}
        <h1 class="identity">{{{ identity }}}</h1>
        {{/identity}}
        {{#tagline}}
        <h2 class="tagline">{{{ tagline }}}</h2>
        {{/tagline}}
      </div>
    </section>

    <section class="section-about" id="about">
      <div class="container">
        <div class="row">
          <div class="col col-8 centered">
            {{{contents}}}
          </div>
        </div>
      </div>
    </section>

  </div>

{{> footer }}
